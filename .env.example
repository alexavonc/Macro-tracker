# Copy to .env and fill in. .env is git-ignored and never shipped to the browser.
# The server proxies Anthropic calls (/api/anthropic) using this key.
ANTHROPIC_API_KEY=

# (Later, for the pixel-art digest loop:)
# OPENAI_API_KEY=
